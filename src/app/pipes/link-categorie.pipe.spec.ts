import { LinkCategoriePipe } from './link-categorie.pipe';

describe('LinkCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new LinkCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
